interface ICard {
   // colorSchema?: 'white' | 'black'
   to: string
   title: string
   photoSrc: string
   city?: string
   beforeTitle?: string
   afterTitle?: string
   text?: string
   date?: Date
}